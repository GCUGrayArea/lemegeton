# Lemegeton Configuration Example
# Copy this file to .env and customize as needed

# ============================================================================
# Redis Configuration
# ============================================================================

# Redis connection URL (overrides host/port if set)
# REDIS_URL=redis://localhost:6379

# Redis host (used if REDIS_URL not set)
# REDIS_HOST=localhost

# Redis port (used if REDIS_URL not set)
# REDIS_PORT=6379

# Redis database number (0-15)
# REDIS_DATABASE=0

# Connection timeout in milliseconds
# REDIS_CONNECT_TIMEOUT=5000

# Enable auto-spawn if Redis unavailable (true/false)
# REDIS_AUTO_SPAWN=true

# Redis retry configuration
# REDIS_RETRY_MAX_ATTEMPTS=10
# REDIS_RETRY_INITIAL_DELAY=500
# REDIS_RETRY_MAX_DELAY=5000
# REDIS_RETRY_FACTOR=2

# ============================================================================
# Docker Configuration
# ============================================================================

# Enable Docker usage (true/false)
# DOCKER_ENABLED=true

# Docker image to use for Redis
# DOCKER_REDIS_IMAGE=redis:alpine

# Container name prefix
# DOCKER_CONTAINER_PREFIX=lemegeton-redis

# Clean up containers on shutdown (true/false)
# DOCKER_CLEANUP_ON_EXIT=true

# Docker socket path (auto-detected if not provided)
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# ============================================================================
# Hub Configuration
# ============================================================================

# Port for hub HTTP API (0 for random)
# HUB_API_PORT=0

# Enable debug logging (true/false)
# HUB_DEBUG=false

# Working directory
# HUB_WORKING_DIRECTORY=/path/to/project

# Task list file path
# HUB_TASK_LIST_PATH=docs/task-list.md

# State sync interval in seconds
# HUB_SYNC_INTERVAL=30

# ============================================================================
# Agent Configuration
# ============================================================================

# Maximum concurrent agents
# AGENT_MAX_CONCURRENT=5

# Agent heartbeat interval in seconds
# AGENT_HEARTBEAT_INTERVAL=30

# Agent timeout in seconds
# AGENT_TIMEOUT=300

# Enable agent debug output (true/false)
# AGENT_DEBUG=false

# ============================================================================
# Global Configuration
# ============================================================================

# Environment (development/production/test)
# NODE_ENV=development

# Log level (error/warn/info/debug/trace)
# LOG_LEVEL=info